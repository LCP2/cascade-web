{
  "_comment": "Per-project parameters for the Codynamics Development Standard. Copy to the repo root and fill in. NO SECRETS — the Atlassian token is set once via 'setx ATLASSIAN_API_TOKEN'. Example values are Cascade's; replace per project.",

  "jiraBaseUrl": "https://codynamics.atlassian.net",
  "jiraEmail": "lee@codynamics.com.au",
  "jiraProjectKey": "CAS",

  "repo": "LCP2/cascade-web",
  "integrationBranch": "staging",
  "productionBranch": "main",

  "pullLabels": ["needs-cc-web"],
  "humanBlockLabel": "needs-lee",

  "verifyCommand": "npm run qa",
  "buildCommand": "python poc_pipeline.py --build-html",
  "deployTarget": "pages",

  "craftDoc": "docs/DESIGN.md",

  "idleSleepSeconds": 120,
  "maxTurns": 200,
  "model": "sonnet"
}
