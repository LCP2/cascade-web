# Codynamics Development Standard — normalise line endings so the tree stops churning.
# Text is stored LF in git and checked out LF; binaries are never touched.
* text=auto eol=lf

*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.ico  binary
*.webp binary
*.woff binary
*.woff2 binary
*.pdf  binary
