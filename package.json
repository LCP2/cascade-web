{
  "name": "cascade-web",
  "private": true,
  "version": "0.0.0",
  "description": "Test tooling for cascade-web, plus the Capacitor iOS wrapper (CAS-453). The web app itself still ships as one built index.html with no build step and no runtime dependencies.",
  "scripts": {
    "build": "python poc_pipeline.py --build-html",
    "test:engine": "node --test tests/js/invariants.test.mjs tests/js/data-integrity.test.mjs",
    "test:data": "python -m unittest discover -s tests",
    "test:monitor": "python -m unittest discover -s monitor/tests",
    "test:e2e": "playwright test",
    "test": "npm run test:engine && npm run test:e2e",
    "qa": "npm run build && npm run test:engine && npm run test:data && npm run test:monitor && npm run test:e2e"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0",
    "@playwright/test": "1.60.0"
  },
  "dependencies": {
    "@capacitor/core": "^8.5.0",
    "@capacitor/ios": "^8.5.0"
  }
}
