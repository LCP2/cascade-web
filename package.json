{
  "name": "cascade-web",
  "private": true,
  "version": "0.0.0",
  "description": "Test tooling for cascade-web. The app itself ships as one built index.html and has no build step and no runtime dependencies — this file exists only so the QA suites (CAS-231, CAS-232) have a home.",
  "scripts": {
    "test:engine": "node --test tests/js/invariants.test.mjs",
    "test:e2e": "playwright test",
    "test": "npm run test:engine && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "1.60.0"
  }
}
